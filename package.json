{
  "name": "vibinet",
  "version": "0.3.0",
  "description": "Deterministic input-synced netcode engine for real-time games.",
  "type": "module",
  "main": "./dist/client.js",
  "module": "./dist/client.js",
  "types": "./dist/client.d.ts",
  "exports": {
    ".": {
      "types": "./dist/client.d.ts",
      "import": "./dist/client.js",
      "default": "./dist/client.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "build:js": "npx esbuild src/client.ts --bundle --format=esm --platform=browser --target=es2020 --outfile=dist/client.js",
    "build:types": "npx tsc src/client.ts --declaration --emitDeclarationOnly --outDir dist --module ESNext --moduleResolution bundler --target ESNext --strict --esModuleInterop --skipLibCheck --allowImportingTsExtensions",
    "build": "npm run build:js && npm run build:types",
    "check:official-endpoint": "bash scripts/check-official-endpoint.sh",
    "prepack": "npm run check:official-endpoint && npm run build",
    "check": "tsc --target esnext --noEmit --skipLibCheck",
    "server": "bun run src/server.ts",
    "deploy": "bash scripts/deploy.sh"
  },
  "keywords": [
    "multiplayer",
    "netcode",
    "rollback",
    "deterministic",
    "games"
  ],
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "esbuild": "^0.25.0"
  }
}
